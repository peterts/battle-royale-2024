<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Acme&display=swap">
    <style>
        body {
            margin: 0;
            padding: 0;
            overflow-x: hidden;
            font-family: 'Comic Sans MS', sans-serif;
        }
        .cartoon-container {
            display: flex;
            justify-content: space-between;
            padding: 10px;
        }
        .frame {
            width: 25%;
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
        }
        .image-box {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            border: 1px solid #ccc; /* Add thin border */
        }
        img {
            max-width: 100%;
            max-height: 100%;
            display: block;
            margin: 0 auto;
        }
        .text-box {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 5px; /* Add padding for better appearance */
        }
        .text {
            text-align: center;
            margin-top: 10px;
        }
        .separator {
            width: 10px;
            background-color: #ccc; /* Line color */
        }
    </style>
    <title>Cartoon Frames</title>
</head>
<body>
    <h1>{{ Tittel }}</h1>
    <div class="cartoon-container">
        {% for bilde in Bilder %}
        <div class="frame">
            <div class="image-box">
                <img src="{{ bilde.Bildelenke }}">
            </div>
            <div class="text-box">
                <div class="text">
                    {% for boble in bilde.Snakkebobler %}
                    '<p>
                        {% if (boble.Person != "Fortellerstemme") %}
                            <strong>{{ boble.Person }}: </strong> "{{ boble.Tekst }}"
                        {% else %}
                            <i>{{ boble.Tekst }}</i>
                        {% endif %}
                    </p>
                    {% endfor %}'
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
</body>
</html>